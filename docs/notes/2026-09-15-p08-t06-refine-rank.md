# P08-T06 — refine/rank AMANDA-RUN-001

Data: 2026-09-15  
Status: PASS no escopo deste worker; gate global com 4 falhas externas registradas  
Run de entrada: `design-engine/runs/AMANDA-RUN-001/run.json`  
Run de entrada preservado: SHA-256 `09cb06cc0a3dcfc9dacad04e8263c72f6cc6fdc2aa8cf56c196a4542c57f54b4`

## Decisão e justificativa

Foram considerados 2 candidatos macro, porque o ranking publicado no P08-T05 contém apenas as duas geometrias únicas viáveis de `AMANDA-RUN-001`. O refinamento separou os seis setores internos dos 260 m² externos, gerou blocos e salas internos e espaços externos de primeira classe, e reexecutou as restrições duras nas resoluções `BLOCK` e `ROOM`. Os dois passaram sem violação dura.

Os dois candidatos foram mantidos como finalistas porque têm geometrias refinadas distintas e formam a fronteira de Pareto não dominada após o refinamento. O corte de até 5 foi aplicado depois da fronteira; nenhuma alternativa foi removida por corte de score ponderado. Como havia somente 2 candidatos refinados viáveis, foram selecionados 2 finalistas:

| Finalista | Pai macro | Seed | Geometria refinada | Score ponderado |
| --- | --- | ---: | --- | ---: |
| `AMANDA-RUN-001-F01` | `COURTYARD:0` | 0 | `5bc3622ee6887108f29d5bcf13f3ec56bec3790d1e0d15f517e161a21c9803ca` | 0.8415491822 |
| `AMANDA-RUN-001-F02` | `COURTYARD:1` | 1 | `f76d48ca8d5624450a2f91e61850b0193608b55b0ae31cb46b41c978d6ef495e` | 0.8415491822 |

Os dois vetores de score são iguais dentro da precisão publicada; a normalização de `program_compliance` evita que um erro residual de ponto flutuante transforme uma igualdade em dominação artificial.

## Números do refinamento

- Macro de entrada: 144 tentativas, 2 válidas, 142 duplicadas, 0 inválidas, 0 desconhecidas.
- Candidatos considerados para o refinamento: 2 de aproximadamente 15, pois só 2 chegaram ao ranking/Pareto do run canônico.
- Refinados: 2.
- Viáveis após BLOCK/ROOM e validação externa: 2.
- Rejeitados após refinamento: 0.
- Fronteira Pareto refinada: `COURTYARD:0`, `COURTYARD:1`.
- Finalistas publicados: 2 de até 5.
- Área interna gerada por solução: 626.000000 m².
- Área externa gerada por solução: 260.000000 m².
- Violações duras publicadas: 0 em cada solução.

Dimensões cruas publicadas em cada `metrics.json`:

| Dimensão | F01 | F02 |
| --- | ---: | ---: |
| `program_compliance` | 1.000000 | 1.000000 |
| `privacy_security` | 0.500000 | 0.500000 |
| `adjacency` | 0.941591 | 0.941591 |
| `circulation` | 0.833333 | 0.833333 |
| `accessibility` | 1.000000 | 1.000000 |
| `solar_heuristic` | `null` | `null` |
| `ventilation_heuristic` | `null` | `null` |
| `green_integration` | 1.000000 | 1.000000 |
| `compactness` | 0.061416 | 0.061416 |
| `constructability` | 1.000000 | 1.000000 |
| `concept_fidelity` | 1.000000 | 1.000000 |

Os pesos congelados são `weights.yaml` versão 1: `program_compliance=0.18`, `privacy_security=0.14`, `adjacency=0.10`, `circulation=0.10`, `accessibility=0.12`, `solar_heuristic=0.06`, `ventilation_heuristic=0.06`, `green_integration=0.06`, `compactness=0.05`, `constructability=0.08`, `concept_fidelity=0.05`. As duas dimensões ambientais ficam explicitamente `null` e fora do rebalanceamento até a passagem ambiental do P08-T07.

## Artefatos

O resumo da etapa está em `design-engine/runs/AMANDA-RUN-001/refinement-summary.json`. Cada diretório abaixo contém `solution.json`, `geometry.geojson`, `adjacency-graph.json`, `flow-graphs.json`, `metrics.json`, `penalties.json`, `validation.json`, `artifact-manifest.json`, `WHY_THIS_OPTION.md`, `floorplan.svg`, `floorplan.png`, `zoning.svg` e `zoning.png`:

- `design-engine/runs/AMANDA-RUN-001/finalists/AMANDA-RUN-001-F01/`
- `design-engine/runs/AMANDA-RUN-001/finalists/AMANDA-RUN-001-F02/`

Os PNGs foram escritos com PIL e verificados como PNGs válidos de 1200×900 em RGB. O GeoJSON é um `FeatureCollection` com 71 features por solução; os IDs de blocos, salas e espaços externos usados em `solution.json` estão presentes no GeoJSON.

## Implementação e testes

Arquivos do worker:

- `src/amanda_agent/design/refinement.py`: refinamento, rechecagem, score, Pareto, seleção e publicação dos artefatos.
- `src/amanda_agent/commands/design.py`: `refine_design_run(root, run_id)` ligado aos inputs canônicos e aos hashes do run.
- `src/amanda_agent/design/models.py`: `DesignSolution.geometry_hash` opcional, validado como SHA-256 hexadecimal.
- `tests/unit/test_design_refine.py`: teste unitário de geração, esquema, coerência de IDs, hash de `run.json` e validade/dimensões dos PNGs.

TDD executado:

1. RED: `python -m pytest tests/unit/test_design_refine.py -q -p no:cacheprovider --basetemp=.tmp-pytest-luna-red` — 1 falha esperada porque `refine_design_run` ainda não existia.
2. GREEN focado: `python -m pytest tests/unit/test_design_cli.py tests/unit/test_design_refine.py tests/solver/test_pipeline.py tests/solver/test_scoring.py tests/solver/test_pareto.py tests/solver/test_rooms.py -q -p no:cacheprovider --basetemp=.tmp-pytest-luna-focused3` — 22 passaram, 0 falharam.
3. Lint: `python -m ruff check src/amanda_agent/design/refinement.py src/amanda_agent/commands/design.py src/amanda_agent/design/models.py tests/unit/test_design_refine.py` — passou.
4. Gate final solicitado: `python -m pytest tests/unit tests/solver -q -p no:cacheprovider --basetemp=.tmp-pytest-luna-b` — 627 passaram, 4 falharam, em 21,60 s. As 4 falhas são de `tests/unit/test_bim_horizun_invoker.py` contra a alteração paralela em `src/amanda_agent/bim/providers/horizun.py`: `AttributeError: 'HorizunInvoker' object has no attribute '_resolved_ids'`. Esse escopo foi preservado e não foi alterado por este worker.

O hash do `run.json` foi comparado antes/depois e permaneceu `09cb06cc0a3dcfc9dacad04e8263c72f6cc6fdc2aa8cf56c196a4542c57f54b4`; nenhum conteúdo do run publicado foi editado.

Os testes específicos de P08-T06 permanecem verdes após a falha externa: 22 passaram, 0 falharam; o lint dos arquivos do worker passou.

## Limitações e riscos conhecidos

- A baixa diversidade é real: o solver produziu somente 2 geometrias macro únicas para este input. Os 142 duplicados continuam contabilizados; não foram fabricadas alternativas e nenhuma regra dura foi relaxada.
- `site.json` continua com limite esquemático `STUDY_PLACEHOLDER`; topografia e norte verdadeiro permanecem ausentes. Os artefatos são estudo, não comprovação cadastral, topográfica ou de implantação final.
- `solar_heuristic` e `ventilation_heuristic` estão `NOT_EVALUATED` neste estágio porque `site.true_north` é `null`. Não há alegação de CFD, Radiance ou simulação detalhada.
- A circulação publicada tem score 0.833333 porque a análise de uma das seis redes encontra a política de acesso residencial no percurso representativo; o resultado e a evidência estão em `flow-graphs.json` e não foram transformados em violação dura.
- O P08-T06 não toca em Revit, BIM ou estado de produção. O Revit vivo permanece sob o orquestrador.

## Retomada para P08-T07

1. Ler `refinement-summary.json` e os dois `metrics.json`/`validation.json` dos finalistas.
2. Executar as heurísticas solar e de ventilação baseadas nas fontes do projeto para os 2 finalistas disponíveis (o conjunto máximo é 5, mas só há 2).
3. Registrar cada saída como `HEURISTIC` ou `SIMULATED`; não promover `null` a simulação e não alegar CFD/Radiance.
4. Atualizar a comparação com as dimensões cruas ambientais, os pesos versão 1 e o total ponderado rebalanceado, preservando esta saída P08-T06 como histórico.
5. Reavaliar a fronteira de Pareto com os valores ambientais quando disponíveis e manter a baixa diversidade explicitamente reportada.

## GitHub e estado de retomada

Não foram executados `git add`, `git commit`, `git push`, `git stash` ou `git checkout`, conforme a instrução do worker. O checkout continua compartilhado e sujo por alterações pré-existentes de outros workers. O orquestrador deve revisar e commitar as mudanças deste escopo junto com este handoff; o grafo de tarefas permanece sob controle do orquestrador.
